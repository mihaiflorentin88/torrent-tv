{
  "name": "@filelist/tv",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "npm run test && tsc --noEmit && vite build"
  },
  "dependencies": {
    "@filelist/shared": "0.1.0",
    "preact": "10.29.8"
  },
  "devDependencies": {
    "@preact/preset-vite": "2.10.6",
    "typescript": "7.0.2",
    "vite": "8.2.2",
    "vitest": "4.1.11"
  }
}
